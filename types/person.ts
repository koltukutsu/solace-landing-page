// import { Any } from "@react-spring/web";
import { StaticImageData } from "next/image";

export type Person = {
    id: number;
    name: string;
    position: any;
    image: StaticImageData;
    linkedin: string;
  };
  